import 'package:flutter/material.dart';

class Recentdeaths extends StatelessWidget {
  const Recentdeaths({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}