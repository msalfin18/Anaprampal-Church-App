import 'package:flutter/material.dart';

class Lectionaryscreen extends StatelessWidget {
  const Lectionaryscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}